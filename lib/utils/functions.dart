

String defaultStringValue(dynamic str) {
  return str != null ? str : '';
}

int defaultNumValue(int num) {
  return num != null ? num : 0;
}

dynamic defaultDynamicValue(dynamic str) {
  return str != null ? str : '';
}